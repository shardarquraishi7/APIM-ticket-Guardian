## next_app

An auxiliary extension used by other extensions to add the App index file as well as `pages/_app.ts` file. This extension should be applied before any extensions that modify the `App` component such as the `UDS` or `next-unleash` extensions. It is not for selection by the user.
